{
  "name": "scrollforge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Spec-driven toolkit for scroll-driven image sequences",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "inspect": "node scripts/inspect-video.mjs",
    "extract": "node scripts/extract-frames.mjs",
    "validate:evidence": "node scripts/validate-evidence.mjs"
  }
}
